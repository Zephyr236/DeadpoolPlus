[listener]#******非特殊情况，默认即可******本地监听端口，其他工具中的SOCKS5代理指定为该IP:PORT，即可轮询使用其他代理
IP='127.0.0.1'
PORT=10086
userName=''#用户名和密码都不为空时，会进行认证，防止部署到vps上时，被其他人使用
password=''
logLevel='normal' # normal:只打印重要信息(代理失效、代理池异常等), debug:额外打印每个请求的代理信息

[task]#周期性任务,若不为空，则会执行（请注意格式）
periodicChecking=''#如0 */5 * * *为每5小时按照[checkSocks]中的配置检查一次内存中已有代理的存活性，如果为空，不单独周期性检查
periodicGetSocks=''#如0 6 * * 6为每周六早上6点去通过fofa、hunter、quake的配置去取数据，顺便一起把本地历史存的也校验一下

[checkSocks]#******非特殊情况，默认即可******
#通过访问实际url来验证代理的可用性
checkURL='https://www.baidu.com/robots.txt'#可以配置为要访问的目标地址，确保所有代理都能访问到目标地址
checkRspKeywords='Baiduspider'#上面地址原始响应中的某个字符串，用来验证通过代理访问目标时有无因某种原因被ban掉。
maxConcurrentReq=200 #同时最多N个并发通过代理访问上面的地址，检测socks5代理是否可用，可根据网络环境调整。云主机的话开500、1000都可以，本机的话，开三五十差不多。
timeout=6 #单位秒，验证socks5代理的超时时间,建议保持在5或6，检查及使用代理访问上面的地址时，超过这个时间，判定无效

[checkSocks.checkGeolocate]##******非特殊情况，默认即可******通过访问返回IP归属地信息的URL和关键字判断，来排除某些代理，如：某些情况下，真正要访问的系统限制只有大陆地区IP可以访问
switch='close' #open:启用，非open:禁用
checkURL='https://qifu-api.baidubce.com/ip/local/geo/v1/district'#访问此url获取IP归属地信息，出于某些原因，建议用国内的公司的相关接口。
#下面的两个的值，需要根据上面url的返回内容填写
excludeKeywords=['澳门','香港','台湾']#格式如：['澳门','香港']优先级最高，返回的body内容中，存在任一关键字，则跳过，
includeKeywords=['中国']#格式如：['中国','北京']则只获取中国北京的代理，如果是['中国'],排除上述关键字的前提下则获取中国所有其他地区代理

[FOFA]
switch = 'open' #open:启用，非open:禁用
apiUrl='https://fofoapi.com/api/v1/search/all'
email = 'hsad@qq.com'
key = '9jz1twi7cucfxf3pdv2e6frm0656bwbx'
# 仅保留查询结果 > 100 条的国家（42/201），经过实际运行筛选，效率提升 ~5x
# 已排除: CN(中国) HK(香港) MO(澳门)
queryStrings = [
    # === 亚洲 (15) ===
    'protocol=="socks5" && country="JP" && banner="Method:No Authentication"',   # 日本      7,574
    'protocol=="socks5" && country="IN" && banner="Method:No Authentication"',   # 印度      7,234
    'protocol=="socks5" && country="KR" && banner="Method:No Authentication"',   # 韩国      4,084
    'protocol=="socks5" && country="ID" && banner="Method:No Authentication"',   # 印尼      3,625
    'protocol=="socks5" && country="IL" && banner="Method:No Authentication"',   # 以色列    3,557
    'protocol=="socks5" && country="SG" && banner="Method:No Authentication"',   # 新加坡    3,051
    'protocol=="socks5" && country="BH" && banner="Method:No Authentication"',   # 巴林      2,628
    'protocol=="socks5" && country="TH" && banner="Method:No Authentication"',   # 泰国      1,810
    'protocol=="socks5" && country="AE" && banner="Method:No Authentication"',   # 阿联酋    1,033
    'protocol=="socks5" && country="MY" && banner="Method:No Authentication"',   # 马来西亚    381
    'protocol=="socks5" && country="TR" && banner="Method:No Authentication"',   # 土耳其      335
    'protocol=="socks5" && country="VN" && banner="Method:No Authentication"',   # 越南        260
    'protocol=="socks5" && country="TW" && banner="Method:No Authentication"',   # 台湾        250
    'protocol=="socks5" && country="BD" && banner="Method:No Authentication"',   # 孟加拉国    168
    'protocol=="socks5" && country="IR" && banner="Method:No Authentication"',   # 伊朗        166
    # === 欧洲 (16) ===
    'protocol=="socks5" && country="DE" && banner="Method:No Authentication"',   # 德国      7,336
    'protocol=="socks5" && country="FR" && banner="Method:No Authentication"',   # 法国      5,836
    'protocol=="socks5" && country="IT" && banner="Method:No Authentication"',   # 意大利    5,218
    'protocol=="socks5" && country="GB" && banner="Method:No Authentication"',   # 英国      4,739
    'protocol=="socks5" && country="ES" && banner="Method:No Authentication"',   # 西班牙    4,455
    'protocol=="socks5" && country="IE" && banner="Method:No Authentication"',   # 爱尔兰    3,848
    'protocol=="socks5" && country="CH" && banner="Method:No Authentication"',   # 瑞士      3,395
    'protocol=="socks5" && country="SE" && banner="Method:No Authentication"',   # 瑞典      3,092
    'protocol=="socks5" && country="NL" && banner="Method:No Authentication"',   # 荷兰        471
    'protocol=="socks5" && country="PL" && banner="Method:No Authentication"',   # 波兰        548
    'protocol=="socks5" && country="FI" && banner="Method:No Authentication"',   # 芬兰        284
    'protocol=="socks5" && country="AT" && banner="Method:No Authentication"',   # 奥地利      238
    'protocol=="socks5" && country="CZ" && banner="Method:No Authentication"',   # 捷克        222
    'protocol=="socks5" && country="RO" && banner="Method:No Authentication"',   # 罗马尼亚    215
    'protocol=="socks5" && country="HU" && banner="Method:No Authentication"',   # 匈牙利      133
    'protocol=="socks5" && country="UA" && banner="Method:No Authentication"',   # 乌克兰      115
    # === 北美洲 (3) ===
    'protocol=="socks5" && country="US" && banner="Method:No Authentication"',   # 美国     10,000
    'protocol=="socks5" && country="CA" && banner="Method:No Authentication"',   # 加拿大    6,911
    'protocol=="socks5" && country="MX" && banner="Method:No Authentication"',   # 墨西哥    3,242
    # === 南美洲 (5) ===
    'protocol=="socks5" && country="BR" && banner="Method:No Authentication"',   # 巴西      6,393
    'protocol=="socks5" && country="AR" && banner="Method:No Authentication"',   # 阿根廷      510
    'protocol=="socks5" && country="CL" && banner="Method:No Authentication"',   # 智利        387
    'protocol=="socks5" && country="VE" && banner="Method:No Authentication"',   # 委内瑞拉    263
    'protocol=="socks5" && country="CO" && banner="Method:No Authentication"',   # 哥伦比亚    165
    # === 非洲 (1) ===
    'protocol=="socks5" && country="ZA" && banner="Method:No Authentication"',   # 南非      3,307
    # === 大洋洲 (2) ===
    'protocol=="socks5" && country="AU" && banner="Method:No Authentication"',   # 澳大利亚  7,344
    'protocol=="socks5" && country="NZ" && banner="Method:No Authentication"',   # 新西兰      184
]
resultSize=10000 #每个查询语句最多返回条数，最大10000，需小于等于FOFA官网web界面看到的结果数量

# HTTP 代理查询（同 42 国），通过常见代理端口 + banner 关键词筛选
httpQueryStrings = [
    # === 亚洲 (15) ===
    '(port="8080" || port="3128" || port="8888" || port="9090") && country="JP" && (banner="proxy" || banner="CONNECT" || banner="squid")',   # 日本
    '(port="8080" || port="3128" || port="8888" || port="9090") && country="IN" && (banner="proxy" || banner="CONNECT" || banner="squid")',   # 印度
    '(port="8080" || port="3128" || port="8888" || port="9090") && country="KR" && (banner="proxy" || banner="CONNECT" || banner="squid")',   # 韩国
    '(port="8080" || port="3128" || port="8888" || port="9090") && country="ID" && (banner="proxy" || banner="CONNECT" || banner="squid")',   # 印尼
    '(port="8080" || port="3128" || port="8888" || port="9090") && country="IL" && (banner="proxy" || banner="CONNECT" || banner="squid")',   # 以色列
    '(port="8080" || port="3128" || port="8888" || port="9090") && country="SG" && (banner="proxy" || banner="CONNECT" || banner="squid")',   # 新加坡
    '(port="8080" || port="3128" || port="8888" || port="9090") && country="BH" && (banner="proxy" || banner="CONNECT" || banner="squid")',   # 巴林
    '(port="8080" || port="3128" || port="8888" || port="9090") && country="TH" && (banner="proxy" || banner="CONNECT" || banner="squid")',   # 泰国
    '(port="8080" || port="3128" || port="8888" || port="9090") && country="AE" && (banner="proxy" || banner="CONNECT" || banner="squid")',   # 阿联酋
    '(port="8080" || port="3128" || port="8888" || port="9090") && country="MY" && (banner="proxy" || banner="CONNECT" || banner="squid")',   # 马来西亚
    '(port="8080" || port="3128" || port="8888" || port="9090") && country="TR" && (banner="proxy" || banner="CONNECT" || banner="squid")',   # 土耳其
    '(port="8080" || port="3128" || port="8888" || port="9090") && country="VN" && (banner="proxy" || banner="CONNECT" || banner="squid")',   # 越南
    '(port="8080" || port="3128" || port="8888" || port="9090") && country="TW" && (banner="proxy" || banner="CONNECT" || banner="squid")',   # 台湾
    '(port="8080" || port="3128" || port="8888" || port="9090") && country="BD" && (banner="proxy" || banner="CONNECT" || banner="squid")',   # 孟加拉国
    '(port="8080" || port="3128" || port="8888" || port="9090") && country="IR" && (banner="proxy" || banner="CONNECT" || banner="squid")',   # 伊朗
    # === 欧洲 (16) ===
    '(port="8080" || port="3128" || port="8888" || port="9090") && country="DE" && (banner="proxy" || banner="CONNECT" || banner="squid")',   # 德国
    '(port="8080" || port="3128" || port="8888" || port="9090") && country="FR" && (banner="proxy" || banner="CONNECT" || banner="squid")',   # 法国
    '(port="8080" || port="3128" || port="8888" || port="9090") && country="IT" && (banner="proxy" || banner="CONNECT" || banner="squid")',   # 意大利
    '(port="8080" || port="3128" || port="8888" || port="9090") && country="GB" && (banner="proxy" || banner="CONNECT" || banner="squid")',   # 英国
    '(port="8080" || port="3128" || port="8888" || port="9090") && country="ES" && (banner="proxy" || banner="CONNECT" || banner="squid")',   # 西班牙
    '(port="8080" || port="3128" || port="8888" || port="9090") && country="IE" && (banner="proxy" || banner="CONNECT" || banner="squid")',   # 爱尔兰
    '(port="8080" || port="3128" || port="8888" || port="9090") && country="CH" && (banner="proxy" || banner="CONNECT" || banner="squid")',   # 瑞士
    '(port="8080" || port="3128" || port="8888" || port="9090") && country="SE" && (banner="proxy" || banner="CONNECT" || banner="squid")',   # 瑞典
    '(port="8080" || port="3128" || port="8888" || port="9090") && country="NL" && (banner="proxy" || banner="CONNECT" || banner="squid")',   # 荷兰
    '(port="8080" || port="3128" || port="8888" || port="9090") && country="PL" && (banner="proxy" || banner="CONNECT" || banner="squid")',   # 波兰
    '(port="8080" || port="3128" || port="8888" || port="9090") && country="FI" && (banner="proxy" || banner="CONNECT" || banner="squid")',   # 芬兰
    '(port="8080" || port="3128" || port="8888" || port="9090") && country="AT" && (banner="proxy" || banner="CONNECT" || banner="squid")',   # 奥地利
    '(port="8080" || port="3128" || port="8888" || port="9090") && country="CZ" && (banner="proxy" || banner="CONNECT" || banner="squid")',   # 捷克
    '(port="8080" || port="3128" || port="8888" || port="9090") && country="RO" && (banner="proxy" || banner="CONNECT" || banner="squid")',   # 罗马尼亚
    '(port="8080" || port="3128" || port="8888" || port="9090") && country="HU" && (banner="proxy" || banner="CONNECT" || banner="squid")',   # 匈牙利
    '(port="8080" || port="3128" || port="8888" || port="9090") && country="UA" && (banner="proxy" || banner="CONNECT" || banner="squid")',   # 乌克兰
    # === 北美洲 (3) ===
    '(port="8080" || port="3128" || port="8888" || port="9090") && country="US" && (banner="proxy" || banner="CONNECT" || banner="squid")',   # 美国
    '(port="8080" || port="3128" || port="8888" || port="9090") && country="CA" && (banner="proxy" || banner="CONNECT" || banner="squid")',   # 加拿大
    '(port="8080" || port="3128" || port="8888" || port="9090") && country="MX" && (banner="proxy" || banner="CONNECT" || banner="squid")',   # 墨西哥
    # === 南美洲 (5) ===
    '(port="8080" || port="3128" || port="8888" || port="9090") && country="BR" && (banner="proxy" || banner="CONNECT" || banner="squid")',   # 巴西
    '(port="8080" || port="3128" || port="8888" || port="9090") && country="AR" && (banner="proxy" || banner="CONNECT" || banner="squid")',   # 阿根廷
    '(port="8080" || port="3128" || port="8888" || port="9090") && country="CL" && (banner="proxy" || banner="CONNECT" || banner="squid")',   # 智利
    '(port="8080" || port="3128" || port="8888" || port="9090") && country="VE" && (banner="proxy" || banner="CONNECT" || banner="squid")',   # 委内瑞拉
    '(port="8080" || port="3128" || port="8888" || port="9090") && country="CO" && (banner="proxy" || banner="CONNECT" || banner="squid")',   # 哥伦比亚
    # === 非洲 (1) ===
    '(port="8080" || port="3128" || port="8888" || port="9090") && country="ZA" && (banner="proxy" || banner="CONNECT" || banner="squid")',   # 南非
    # === 大洋洲 (2) ===
    '(port="8080" || port="3128" || port="8888" || port="9090") && country="AU" && (banner="proxy" || banner="CONNECT" || banner="squid")',   # 澳大利亚
    '(port="8080" || port="3128" || port="8888" || port="9090") && country="NZ" && (banner="proxy" || banner="CONNECT" || banner="squid")',   # 新西兰
]

[QUAKE]
switch = 'close'
apiUrl='https://quake.360.net/api/v3/search/quake_service'
key = '96*****36-*****-40c4-*****-5ef*****90b0a'
queryString = 'service:socks5  AND country: "CN" AND response:"No authentication"'
resultSize=500 #此处最大为10000,需小于等于从官网web界面看到的结果数量

[HUNTER]
switch = 'close'
apiUrl='https://hunter.qianxin.com/openApi/search'
key = '9c1698e0*****90758edc2******533f9*******048***ce18'
queryString = 'protocol=="socks5"&&protocol.banner="No authentication"&&ip.country="CN"'
resultSize=200 #最小为100,按100条/每页翻页，最大值需小于从官网web界面看到的结果数量，值需为100的整倍数，如200、300、1000、2000等